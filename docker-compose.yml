services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - NODE_ENV=production
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
